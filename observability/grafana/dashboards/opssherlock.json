{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "type": "timeseries",
      "title": "Checkout request rate",
      "targets": [{"expr": "rate(checkout_requests_total[1m])", "legendFormat": "{{status}}", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Checkout p95 latency",
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le) (rate(checkout_request_duration_seconds_bucket[1m])))", "refId": "A"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Injected fault",
      "targets": [{"expr": "checkout_fault_active", "legendFormat": "{{fault}}", "refId": "A"}],
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8}
    }
  ],
  "schemaVersion": 40,
  "tags": ["opssherlock"],
  "templating": {"list": []},
  "time": {"from": "now-15m", "to": "now"},
  "timezone": "browser",
  "title": "OpsSherlock Incident Lab",
  "uid": "opssherlock"
}
